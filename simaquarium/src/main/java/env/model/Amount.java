package env.model;

/**
 * The `Amount` enum represents the quantity of a resource or entity in the simulation.
 * It is used to define different levels of quantity, such as:
 * - `LITTLE`: Represents a small amount.
 * - `NORMAL`: Represents a moderate or average amount.
 * - `MANY`: Represents a large amount.
 */
public enum Amount {
    LITTLE,
    NORMAL,
    MANY
}
