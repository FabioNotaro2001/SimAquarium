package env.model;

/**
 * The `Direction` enum represents the four cardinal directions in the simulation.
 * It is used to define the orientation or movement of agents within the aquarium environment.
 * The directions are:
 * - `TOP`: Represents the upward direction.
 * - `LEFT`: Represents the leftward direction.
 * - `BOTTOM`: Represents the downward direction.
 * - `RIGHT`: Represents the rightward direction.
 */
public enum Direction {
    TOP,
    LEFT,
    BOTTOM,
    RIGHT
}
